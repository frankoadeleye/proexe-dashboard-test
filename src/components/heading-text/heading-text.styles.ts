import styled from "styled-components";
import { FontSizeFor } from "utils/functions";

export const HeadingTextBlock = styled.header`
  font-weight: bold;
  ${FontSizeFor.HeaderText}
  width: auto;
  margin: 30px 0px;
`;
